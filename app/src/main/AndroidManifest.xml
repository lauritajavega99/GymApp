<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gymapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GymApp">
        <activity android:name=".Calendario"></activity>
        <activity android:name=".Entreno_Diario" />
        <activity android:name=".Opciones" />
        <activity android:name=".EleccionEjercicio" />
        <activity android:name=".VistaEjercicio" />
        <activity android:name=".ListaEjercicios" />
        <activity android:name=".ListaEjercicios1" />
        <activity android:name=".ListaEjercicios2" />
        <activity android:name=".ListaEjercicios3" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver android:name=".NotificacionAlarma" />
    </application>

</manifest>